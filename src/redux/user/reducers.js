import {} from './actions';

const initialState = {};

function userRuducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default userRuducer;
